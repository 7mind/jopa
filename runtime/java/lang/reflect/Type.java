package java.lang.reflect;

/**
 * Type is the common superinterface for all types in Java.
 * These include raw types, parameterized types, array types, type variables
 * and primitive types.
 */
public interface Type {
    // Note: In Java 8+, getTypeName() is a default method.
    // For Java 7 compatibility, we omit it.
}
